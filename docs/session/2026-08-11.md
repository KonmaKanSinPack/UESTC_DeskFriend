# 2026-08-11 TTS 方案定型：SiliconFlow API 转正，本地 CosyVoice2 标记未完成

## 背景
本地 CosyVoice2（昨天接入）合成乱码 + 速度不稳定（6.7s~386s/句）。

## 排查结论（根因）
- 现象：合成流程通但内容完全无关 + 音色不像（官方示例音频也乱）
- 排除：参考音频（换干净 5.9s + 精确转写）/ 模型文件完整性 / whisper 版本（20231117）/
  fp16（官方环境 fp16=True 正常）
- 根因：**版本栈整体漂移**——主环境 Python 3.12 + torch 2.11 + onnxruntime 1.24 + numpy 2.x
  与官方矩阵（3.10 + torch 2.3.1 + onnxruntime 1.18 + numpy 1.26 + whisper 20231117 +
  transformers 4.51.3 + x-transformers 2.11.24）数值行为不同 → speech token 全错
- 单点降级不可行：transformers 5.x 要 torch 2.4+（register_fake）、x-transformers 2.25 要
  torch 2.5+、torch 2.3.1 + transformers 4.51.3 又缺 register_fake——互相锁定
- 官方环境（.venv-cosyvoice，Python 3.10 + requirements 全量）验证**正常**，但速度仍不稳
  （195s~386s 复现），且需独立环境常驻

## 决策（用户拍板）
放弃本地方案作为主路径，改用 **SiliconFlow API**（官方托管 CosyVoice2-0.5B）：
- POST /v1/audio/speech，`references` 参数每次传参考音频 base64 + 文本 → 零样本克隆
- response_format=wav 直接 sounddevice 播放；1.6s/句稳定
- 音色验证：胡桃参考音频克隆效果满意

## 改动
- `tts.py`：新增 SiliconFlowTTS（逐句请求 + 播放 + 打断复用）；工厂加 siliconflow 分支；
  CosyVoice2TTS docstring 标记"⚠️ 未完成"（保留待完善：官方环境常驻服务 + vllm）
- `config.toml` / `config.example.toml`：TTS_BACKEND=siliconflow + TTS_API_KEY/BASE_URL/MODEL/
  SAMPLE_RATE + 参考音频键
- `tests/test_tts.py`：SiliconFlowTTS 4 用例（references 请求构造、base64 缓存、缺配置容错）
- `.gitignore`：+`.venv-cosyvoice/` +`assets/models/`（3.3GB 模型不入库）
- 参考音频入库：assets/vo_hutao_mimitomo_friendship2_01.wav（桃桃音色资产）

## 验证
109 passed；真机 SiliconFlowTTS 合成播放成功（用户听感满意）。

## 待办
- [ ] 本地 CosyVoice2 完善（官方环境常驻 + vllm）——标记未完成，非当前优先
- [ ] 打断机制真机验证（朗读中说话 → 停止 + 打断位置进 context）

---

## TTS 文档编写计划（设计思路，待确认后实施）

### 目标
让使用者能按文档配置/更换 TTS（换音色、切后端），开发者理解打断机制与决策背景。

### 涉及文件
- `README.md`：用户面向——能力列表、配置说明、新增"语音输出（TTS）"章节、玩法表
- `docs/DEVELOPMENT.md`：决策面向——非目标更新、新增 TTS 章节（架构 + 决策 + 本地版状态）

### README.md 改动设计

**① 能力列表**补一行（与"听觉"对称）：
```
- **语音输出**：桃桃回复自动朗读（SiliconFlow 托管 CosyVoice2，可克隆自定义音色）；朗读中你说话会立即打断，桃桃知道你说到哪
```

**② 配置段**补 TTS 键：
```toml
TTS_BACKEND = "siliconflow"   # siliconflow（默认，API 托管）/ cosyvoice2（本地，未完成）/ dummy / none
TTS_API_KEY = "..."           # SiliconFlow API key
TTS_VOICE_REF = "assets/xxx.wav"      # 参考音频（3-10 秒干净人声）
TTS_VOICE_REF_TEXT = "..."            # 参考音频文本（必须与音频逐字一致！）
```

**③ 新增"语音输出（TTS）"章节**，含三个小节：
- **方案**：SiliconFlow API（默认，稳定 1~2s/句）| 本地 CosyVoice2（未完成）| dummy（静音）
- **换音色指南**（核心可操作内容）：
  ```
  1. 准备 3-10 秒干净人声（无 BGM/混响），放 assets/
  2. 转写参考音频文本（可用 faster-whisper），人工逐字核对——文本与音频
     必须精确对应，错一个字克隆质量就崩（本项目踩过）
  3. 填 TTS_VOICE_REF + TTS_VOICE_REF_TEXT，重启生效
  ```
- **打断机制**：朗读中说话/打字 → 立即停止 → 下条消息自动带
  「[对话被打断] 你刚才说到『…』处被打断了」→ 桃桃感知并衔接

**④ 玩法表**加两行：
```
| 桃桃回复时 | 自动朗读（可打断：说话即停，桃桃知道说到哪） |
```

### DEVELOPMENT.md 改动设计

**① 非目标**：删除"TTS 语音合成（可列为远期候选）"——已实现（2026-08-11）

**② 新增 §8 TTS 语音输出**（决策记录）：
- **决策**：主方案 SiliconFlow API（官方托管 CosyVoice2-0.5B）——`references` 参数
  每次传参考音频 base64 实现零样本克隆；1.6s/句稳定
- **决策背景（教训）**：本地 CosyVoice2 对版本栈极度敏感（3.12+新版依赖 vs 官方
  3.10 矩阵数值行为差异 → 乱码+速度不稳），单点降级被依赖互锁挡住；官方环境验证
  正常但需独立环境常驻。标记"未完成"，待官方环境常驻 + vllm 加速
- **架构**：`tts.py` 的 `TTS` 抽象（speak/interrupt/busy/played_text）+ 工厂装配；
  逐句合成播放 = 句粒度打断进度；打断位置经 `brain.set_interruption` 注入对话上下文

### 关键接口片段（文档内引用）
```python
class TTS(ABC):
    async def speak(self, text: str) -> None      # 合成+播放（可打断）
    async def interrupt(self) -> str              # 打断，返回已播文本前缀（打断位置）
    @property
    def busy(self) -> bool                        # 是否正在朗读
# 打断位置注入协议（astrbot 后端 get_llm_response）
f"[对话被打断] 你刚才说到『{prefix}』处被打断了。用户现在说：{message}"
```

### 验证
文档改完 ruff/测试不受影响（纯文档）；README 渲染检查（无断链）。
