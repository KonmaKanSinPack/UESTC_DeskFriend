# 2026-09-04 会话:hotfix——重启后图片占位符毒化上下文致 API 500

## 问题(用户报错)

```
处理消息时出错了：Error code: 500 - {'error': {'message': "get file data from
'base64:[image omitted]' failed: failed to decode base64 data: illegal base64
data at input byte 0 ...", 'code': 'convert_request_failed'}}
```

## 根因链(已核实代码)

1. `look_at_screen` 工具消息带 base64 图进 openai 后端 context;
2. `memory.sanitize_message` 落库时把 `image_url.url` 替换为 `"[image omitted]"`
   占位符(防 pet.db 膨胀,Phase 5 设计);
3. **重启**后 `OpenAIBackend.__init__` 经 `memory.load_unsummarized()` 恢复上下文,
   占位符原样回到 `self.context`;
4. 此后每轮 `get_response` 把全量 context 发 API——网关把 `"[image omitted]"`
   当 base64 数据解码 → 500 convert_request_failed,**毒化重启后所有对话**。

## 修复(commit cc4211e)

在"库 → 运行时"还原边界加 `revive_message`(单点,memory.py):
- 判定:url 不以 `data:` 开头的图片段 = 非法(真实内存图片恒为 data: URL)
- 动作:换成 `{"type": "text", "text": "[历史截图，内容已省略]"}` 文本段
  (历史对 LLM 仍可读,不再有非法 image_url)
- `load_unsummarized` 返回前统一过这道转换;DB 存量数据(已带占位符)下次启动
  自动被修复,**无需迁移**

## 验证

- 新增 4 用例(占位符→文本段/原消息不改/data: 通过/纯文本通过/store 全链路
  roundtrip 无非法 image_url);全量 **200 passed**,ruff 全绿
- technical.md 数据结构小节同步(核心不变式补"恢复过 revive_message")

## 未完成/下一步

- 用户重启应用即生效;若仍复现 500 请把新报错发我(可能是别的消息形态)
- 沿用 PROGRESS 待办:openai 流式管道轮(首选)/Live2D 轮/动画渲染改造/P1/AEC

---

# 2026-09-04 会话(第二轮):config 重构——三文件分层 + 记忆阈值配置化

## Plan + Spec(方案 A 三文件已经用户确认)

### 背景

用户指出配置一锅粥(openai 模式下 astrbot 键全是噪音),且记忆阈值硬编码
(memory.py 定义自己不用的常量、与项目 config→工厂→构造参数的既有惯例脱节,
与 MAX_FACTS 放法不一致)。确认结构:

```
config/
  common.toml     # BACKEND 选择器 + 共享:ASR_*/TTS_*/SPRITE/JUDGE_*/MEMORY_*
  openai.toml     # API_KEY/BASE_URL/OPENAI_MODEL/SYSTEM_PROMPT
  astrbot.toml    # ASTRBOT_*/SCREEN_*
```

合并规则:common 读 BACKEND → 加载对应后端文件 → 后端键覆盖同名共享键。

### 任务

| # | 任务 | 要点 |
|---|---|---|
| 1 | `config_loader.py`(新) | `load_config(config_dir=None)`;缺文件抛 ConfigError 带修复指引;load_config 从 skin.py 迁来(配置加载是装配层职责,非皮的) |
| 2 | 记忆阈值配置化 | OpenAIBackend +max_context_turns/keep_recent_turns/max_facts 构造参数,默认值 `DEFAULT_*` 移到 openai.py(策略 owner);memory.py 删两个自用为零的常量;工厂读 MEMORY_MAX_TURNS/MEMORY_KEEP_RECENT/MEMORY_MAX_FACTS |
| 3 | openai 分支过 config | create_backend 的 `OpenAIBackend()` 裸构造(自己偷读 config.toml)改为显式传 config——配置统一走工厂 |
| 4 | 活配置迁移 | 现有 config.toml(含密钥)拆三文件;判定器兜底密钥 `API_KEY="lm-studio"` 放 common(本地端点任意串;openai 模式被真实 key 覆盖);删旧 config.toml |
| 5 | example 三件套 | config/{common,openai,astrbot}.example.toml 入库;删根 config.example.toml;gitignore 改 `config/*.toml` + `!*.example.toml` |
| 6 | 测试 | tests/test_config_loader.py(合并/选择/缺文件指引/后端覆盖同键);test_memory 常量导入改 backends.openai |
| 7 | 文档 | DEVELOPMENT 决策 16+配置小节、technical §4 键清单、README 配置段、overall、PROGRESS |

### 验收

pytest 全绿(+新用例);`uv run python -c "from config_loader import load_config; print(load_config()['BACKEND'], load_config()['OPENAI_MODEL'])"` 用活配置实测;全仓无残留 `config.toml` 引用(历史日志除外);密钥不出日志。

## 收尾日志(第二轮,完成后补写)

(待补)
